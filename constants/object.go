package constants

const (
	// ObjectDomain identifies domain objects.
	ObjectDomain = "domain"

	// ObjectContact identifies contact objects.
	ObjectContact = "contact"

	// ObjectHost identifies host objects.
	ObjectHost = "host"
)
